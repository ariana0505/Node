// argumentos de entrada
//console.log(process.argv)

// controlar el proceso y su salida 0 es que todo esta bien y 1 hubo error
// process.exit(1)

// podemos controlar eventos del proceso
// process.on('exit', () => {
//   // limpiar los recursos
// })

// current working directory
console.log(process.cwd()) //desde que carpeta se a ejecutado del directorio

// platform
console.log(process.env.PEPITO)